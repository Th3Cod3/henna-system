{% extends 'layout.twig' %}
{% block content %}
<div class="container-fluid">
	<div class="row my-2">
		{# Assitance #}
		<div class="col-12">
			<div class="card bg-light mt-3">
				<div class="card-header">
					<h4 class="card-title">Cost</h4>
				</div>
				<a href="{{ 'events/addcost/' | url }}{{ id }}" class="btn btn-outline-info btn-lg btn-block rounded-0">Add Cost</a>
				<div class="card-body">
					
				</div>
			</div>
		</div>
	</div>
</div>
{% endblock %}